
/* This is for the Download button
... I used // for a comment. Now to try again.
That apparently wasn't it. I guess I should just restart. */

$(function()  {
  $(".btn").click(function()  {
    $(".submenu").slideToggle(500);
  });
});
